# crossctl

Level-crossing controller with an automated integration test bench.

**Stack:** C++17 (state machine, TCP server), C11 (telegram framing, CRC-16),
GNU Make (build profiles), Bash (integration harness), Docker (reproducible toolchain).

Work in progress.
